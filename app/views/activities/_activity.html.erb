<tr>
	<td align="center"><%= activity.order %></td>
	<td align="center"><%= activity.role.name %></td>
	<td><b><%= activity.title %></b><br><%= simple_format(activity.description) %></td>
	<td><%= link_to 'Edit', edit_cprocess_activity_path(@cprocess,activity) %></td>
	<td><%= link_to 'Destroy', cprocess_activity_path(@cprocess,activity), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>